const PI float = 3.141592653589793

func fac(int number) (int) {
    var NewNumber int = 1
    var CI int = number
    while CI > 0 {
        NewNumber = NewNumber * CI
        CI = CI-1
    }
    return NewNumber
}

func exp(float number)

func power(float base float expondent) (float) {
    house value = 0.0

}